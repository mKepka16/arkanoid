class Block {
  constructor(textureId, gridPosition, position, width, height) {
    this.textureId = textureId;
    this.gridPosition = gridPosition;
    this.position = position;
    this.width = width;
    this.height = height;
  }
}

export default Block;
